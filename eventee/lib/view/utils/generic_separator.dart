
import 'package:flutter/material.dart';

class GenericSeparator extends StatelessWidget {
  const GenericSeparator();

  @override
  Widget build(context) {
    return const Divider(
      color: Colors.black54,
      thickness: 1.0,
    );
  }
}